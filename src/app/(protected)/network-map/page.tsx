"use client";

import NetworkMap from "@/app/(protected)/network-map/components/NetworkMap";
import React from "react";

const page = () => {
  return (
    <div>
      <NetworkMap />
    </div>
  );
};

export default page;
